apply plugin: 'java'

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]

dependencies {
    compile project(":core/core")
    compile project(":modules/Quest")
}
